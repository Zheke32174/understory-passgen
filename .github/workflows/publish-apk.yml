name: Android validation — no publication

on:
  pull_request:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: android-validation-${{ github.repository }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
          show-progress: false
      - uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@3f131e8634966bd73d06cc69884922b02e6faf92
        with:
          gradle-version: '8.10.2'
          cache-provider: basic
      - name: Validate public signing boundary
        run: python ci/validate-public-signing-boundary.py
      - name: Install Android SDK components
        run: yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --install "platforms;android-35" "build-tools;35.0.0" > /dev/null
      - name: Assemble local debug APK
        shell: bash
        run: |
          set -o pipefail
          if ! gradle --no-daemon assembleDebug --console=plain > assemble.log 2>&1; then
            tail -n 250 assemble.log
            exit 1
          fi
          tail -n 80 assemble.log
      - name: Run unit tests with bounded failure receipt
        shell: bash
        run: |
          set -o pipefail
          if ! gradle --no-daemon test --console=plain > unit-test.log 2>&1; then
            tail -n 250 unit-test.log
            exit 1
          fi
          tail -n 80 unit-test.log
