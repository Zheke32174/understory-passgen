# Robolectric 4.13 ships SDK stubs up to API 34; the project's
# compileSdk/targetSdk is 35. Pin the test runtime to 34 so every test
# class doesn't have to repeat @Config(sdk = [34]). Tracked as a
# RELEASE-aligned pin: when Robolectric ships an API 35 stub bump this
# (and the aegis copy) to 35.
sdk=34
