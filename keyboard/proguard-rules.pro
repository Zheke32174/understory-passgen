# AIDL stubs must keep their descriptors so both sides of the plugin IPC
# agree on the interface (see keyboard-plugin-api/consumer-rules.pro,
# which already keeps the api package; nothing extra needed here yet).
